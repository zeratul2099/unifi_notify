language: python
python:
    - "3.5"
    - "3.6"
    - "3.7-dev"

cache: pip

install:
    - "pip install pipenv"
    - "pipenv install"
    - "pip install pylint"

script:
    - "pipenv run python -m py_compile *.py"
    - "pipenv run pylint *.py -f colorized"
